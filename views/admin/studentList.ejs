<%- include('../partials/header')%>
<%- include('../../js/textDisplay.ejs')%>

<div class="container col-12 col-sm-11 col-md-10 col-lg-9 col-xl-8">
<div class="card mb-4">
  <div class="card-body">
    <h3 class="card-title text-center">Listes des étudiant</h3>
    <table id="studentListTable" class="table table-striped table-sm" cellspacing="0" width="100%">
      <thead>
        <tr>
          <th scope="col">Etudiant</th>
          <th scope="col">Etat des recherches de stage</th>
        </tr>
      </thead>
      <tbody>
        <% for(const student of students){ %>
          <tr>
            <th class="text-capitalize" scope="row"><a href="/admin/students/<%=student._id%>"><%=student.lastName + " " + student.firstName%></a></th>
            <td><%-booleanToText(student.foundInternship)%></td>
          </tr>
        <% } %>
      </tbody>
    </table>
  </div>
</div>
<%- include('../partials/footer')%> 